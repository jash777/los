/**
 * Constants Index
 * Centralized exports for all constants
 */

const workflow = require('./workflow');
const loanOriginationPhases = require('./loan-origination-phases');

module.exports = {
  workflow,
  loanOriginationPhases
};